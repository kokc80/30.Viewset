\\активируем виртуальное окружение
	.\env\Scripts\activate
pip freeze > requirements.txt копирование списка зависимостей
pip install -r requirements.txt  установка зависимостей

\\создаем проект
	django-admin startproject config .
\\создаем виртуальное окружение
	python -m venv env
\\активируем виртуальное окружение
	.\env\Scripts\activate

	pip install django
	pip install psycopg2-binary
	pip install pillow
	pip install djangorestframework
	pip install python-dotenv	
	
	python manage.py startapp users
	python manage.py migrate auth zero
	
	pip install coverage 
	pip install -U drf-yasg
	"django-insecure-0tv-8@q6ehjq4_3hvz$3t1tn20#8#c*1=qya!f4$@=@_5dw1hi"

    pip install stripe
    pip install forex-python
	# 32.2 Документирование и безопасность АльтЗ.mp4 #
	22:00 kokc@inbox.ri 678330-Pnp
	30:00 su@admin.ru 123qwe

    pip install django_celery_beat
	python manage.py migrate django_celery_beat